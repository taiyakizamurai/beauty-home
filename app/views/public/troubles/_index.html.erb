
   <table class="table table-hover table-inverse">
        <thead>
      <tr>
        <th><p>お悩み検索<p></th>
      </tr>
    </thead>
    <tbody>
      <% troubles.each do |trouble| %>
         <tr>
           <td><%= link_to trouble.name, trouble_path(trouble.id) %></td>
         </tr>
       <% end %>
    </tbody>
  </table>